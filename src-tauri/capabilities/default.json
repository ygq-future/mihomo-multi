{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capabilities for the app",
  "windows": ["main", "*"],
  "permissions": [
    "core:default",
    "shell:default",
    "process:default"
  ]
}
