# 🚀 Mihomo Multi v1.1.0 发布说明

**基于 Mihomo (Clash.Meta) 内核与 Tauri v2 构建的轻量级高性能多端口代理监听绑定桌面客户端。**

---

## 💡 本次版本核心更新亮点

v1.1.0 带来了一系列架构级稳定性改进、资源底噪优化与关键交互体验升级：

1. 🛡️ **内核崩溃防断网直连熔断与系统代理自愈机制**：
   - 实现了对 Mihomo 伴生内核进程的毫秒级心跳与退出监视。当内核意外崩溃或退出时，系统代理将自动熔断清理，绝不导致系统全局断网；内核拉起自愈后，将自动恢复先前绑定的系统代理状态。
2. ⚡ **性能与资源占用深度调优**：
   - **国内分流规则升级**：全量迁移至经过高度裁剪的完整 MRS 二进制规则集，大幅降低 Mihomo 内核在复杂网络路由匹配时的内存开销；
   - **冗余写入与热重载消除**：增加对配置变更的精准指纹对比，消除未实质变更时的重复磁盘写 I/O、无意义的 API 热重载以及开机自启动重复调用；
   - **轮询查询优化**：修复备用状态轮询循环，实现对系统托盘状态查询结果的高效复用，彻底降低 UI 闲置时的 CPU 唤醒开销。
3. 🎛️ **交互与端口状态控制重构**：
   - 增加全新 `HoverStepSlider` 多阶段状态控制器，消除跨阶段切换时的视觉反弹与抖动；
   - 优化系统代理下拉选择交互联动与状态实时同步；
   - 托盘悬浮信息增强，悬停时可实时获知当前端口启用数与活动系统代理端口状态。
4. 📝 **运维与日志可靠性**：
   - 新增内核日志按天自动轮转功能，默认安全保留 30 天，杜绝长期运行导致的日志磁盘膨胀。

---

## 📦 全平台分发包下载指引

| 操作系统 / 架构 | 分发包文件格式 | 产物推荐文件名 | 适用场景与说明 |
| :--- | :--- | :--- | :--- |
| **Windows x64** | `.exe` (NSIS) | `mihomo-multi_1.1.0_x64-setup.exe` | **推荐**。主流 64 位 Windows 用户安装包，带桌面/开始菜单快捷方式与卸载程序 |
| | `.zip` (Portable) | `mihomo-multi_1.1.0_windows-x64-portable.zip` | **推荐**。绿色免安装便携包，解压即用，配置随目录走，与指纹浏览器高度契合 |
| | `.msi` | `mihomo-multi_1.1.0_x64_en-US.msi` | Windows 企业级批量静默部署安装包 |
| **Windows ARM64** | `.exe` (NSIS) | `mihomo-multi_1.1.0_arm64-setup.exe` | 适配 Snapdragon X Elite / Surface Pro 等 Windows on ARM 设备 |
| | `.zip` (Portable) | `mihomo-multi_1.1.0_windows-arm64-portable.zip` | Windows on ARM 绿色免安装便携包 |
| **macOS (Apple Silicon)** | `.dmg` | `mihomo-multi_1.1.0_aarch64.dmg` | 针对 M1/M2/M3/M4 系列 Mac，原生 ARM64 极致性能与低能耗 |
| **macOS (Intel)** | `.dmg` | `mihomo-multi_1.1.0_x64.dmg` | 针对老款采用 Intel 芯片的 Mac 电脑 |
| **macOS (Universal)** | `.dmg` | `mihomo-multi_1.1.0_universal.dmg` | 双架构通用安装包，免去区分架构的识别成本 |
| **Linux x86_64** | `.AppImage` | `mihomo-multi_1.1.0_amd64.AppImage` | **推荐**。全 Linux 发行版通用单文件可执行包，无需安装依赖 |
| | `.deb` | `mihomo-multi_1.1.0_amd64.deb` | Ubuntu / Debian / Deepin 等基于 dpkg 的发行版安装包 |
| | `.tar.gz` (Portable) | `mihomo-multi_1.1.0_linux-x64.tar.gz` | Linux 独立免安装绿色归档包 |

*注：每个分发包的 SHA-256 校验和均记录在同目录的 `SHA256SUMS.txt` 文件中，下载后可核对完整性。*

---

## 🍏 macOS 首次安装提示“已损坏，无法打开”或“未知开发者”的解决方案

由于本开源项目尚未购买 Apple Developer 开发者付费证书（$99/年），macOS 的 Gatekeeper（门禁系统）会对从网络下载的未公证应用施加 `com.apple.quarantine` 隔离属性，导致系统弹出警告：**“Mihomo Multi 已损坏，打不开。您应该将它移到废纸篓”** 或 **“无法打开，因为无法验证开发者”**。

这并非文件真正损坏，请通过以下任意一种方式即可正常打开运行：

### 方法 1：一键终端命令解除隔离（推荐，最彻底）

将 `Mihomo Multi.app` 拖入 `应用程序 (Applications)` 文件夹后，打开 macOS 的 **终端 (Terminal)**，复制并执行以下命令：

```bash
sudo xattr -rd com.apple.quarantine /Applications/Mihomo\ Multi.app
```
*执行时需输入当前 Mac 用户锁屏密码（输入时终端不显字符，直接按回车即可）。命令完成后即可正常双击启动应用。*

### 方法 2：通过系统设置安全策略允许运行

1. 双击打开应用，弹出无法打开的拦截对话框后，点击 **取消**；
2. 打开系统 **“系统设置” -> “隐私与安全性”**；
3. 向下滚动至 **“安全性”** 区域，会看到提示：“已阻止使用‘Mihomo Multi’，因为来自身份不明的开发者”；
4. 点击旁边的 **“仍要打开”** 按钮，并在随后弹出的确认框中点击 **“打开”** 并输入锁屏密码即可。
