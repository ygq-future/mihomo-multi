# 🚀 Mihomo Multi-Port v1.0.0 正式首发发布

**基于 Mihomo (Clash.Meta) 内核与 Tauri v2 构建的轻量级高性能多端口代理监听绑定桌面客户端。**

---

## 💡 产品定位与核心价值

传统主流代理桌面客户端主要面向个人日常上网分流，通常仅开放单一全局混合代理端口（如 `7890`）。

当您在以下业务场景中，需要**多个独立的本地监听端口分别绑定到不同的出站节点**时：
- 🌐 **指纹浏览器多开防关联**（如 ADS/Hubstudio/BitBrowser 等环境隔离，要求 `7891 -> 🇯🇵 日本01`，`7892 -> 🇭🇰 香港02`，`7893 -> 🇺🇸 美国01`）；
- 🤖 **海外社交媒体与跨境电商多账号自动化运营**；
- 🕷️ **分布式网络爬虫与数据采集**；
- 🔬 **跨地域多环境网络连通性与可用性测试**。

传统方案往往需要启动多个笨重的客户端软件，导致系统资源极度浪费、配置繁琐且容易串线。

**Mihomo Multi-Port** 专为解决该痛点而生，采用极致聚焦的做减法架构设计：
- 🎯 **单一核心职责**：专注于 `本地多端口监听 -> 1:1 精确绑定指定节点`。
- ⚡ **超轻资源底噪**：由单个受控的 Mihomo Sidecar 内核统一调度，后台内存占用低至 ~30MB，告别多开臃肿客户端。
- 🔄 **毫秒级配置热重载**：新增、修改、删除或启停端口映射时，均通过 Mihomo REST API 实时无缝热加载，**不重启内核进程，不断开现有长连接**。
- 🛡️ **确定性路由原则**：每一个端口均严格通过 `IN-PORT,<port>,<node>` 映射至指定节点，禁止隐式模糊切换或自动轮询漂移，确保多环境隔离的出口 IP 绝对确定性。
- 🚀 **零冗余包袱**：不引入 TUN 虚拟网卡驱动，不引入复杂的 JavaScript/Lua 脚本合并与多层分流规则，轻巧稳定。

---

## ✨ 核心特性一览

### 1. 多端口独立监听与 1:1 精确绑定
- **灵活入站协议**：支持配置任意数量的本地入站端口，支持 Mixed（混合代理）、纯 HTTP、纯 SOCKS5 协议。
- **端口冲突前置探测**：在保存或启用端口前，自动调用系统网络栈探测本地端口可用性，防止端口占用导致冲突。
- **独立启停与编辑**：各端口独立受控，支持单独启用、停用、切换绑定节点或修改描述。
- **一键快捷复制**：集成快捷复制菜单，一键复制 `127.0.0.1:<端口>`、`http://...`、`socks5://...` 或可直接在终端运行的 cURL 代理测试命令。

### 2. 受控系统代理与防断网守护
- **严格单选互斥**：系统代理全局严格单选，至多同时绑定 1 个已启用的监听端口，状态清晰可溯。
- **智能 Bypass 绕过名单**：内置私有网络与回环地址（`localhost`、`127.*`、`10.*`、`192.168.*` 等），支持用户自定义追加绕过域名或 IP。
- **系统环境变量联动**：Windows 平台下开启系统代理时，可选联动设置当前用户级环境变量（`all_proxy`、`http_proxy`、`https_proxy`、`no_proxy`），全面覆盖终端与命令行工具。
- **生命周期安全红线**：当绑定端口停用/删除、内核停止或客户端正常退出/异常崩溃时，自动强制清理系统代理与环境变量设置，**彻底杜绝断网残留**。
- **Windows UWP 回环豁免管理**：内置针对 Windows UWP 应用的回环代理豁免检测与一键修复管理。

### 3. 订阅与配置闭环管理
- **多样化导入方式**：支持远程订阅 URL 下载拉取（携带标准兼容 User-Agent）与本地 Clash YAML 文件导入。
- **自动化静默同步**：支持设置后台定时自动更新间隔，亦支持一键手动全量同步。
- **订阅内置节点检视**：内置卡片式弹窗，无需切换页面即可快速查看特定订阅下解析出的全部节点列表与类型。

### 4. 节点仪表盘与高并发真实测速
- **可视化网格呈现**：卡片式直观呈现所有解析节点，自动识别并显示所属国家/地区旗帜与协议类型徽章（Shadowsocks、VMess、Trojan、VLESS、Hysteria2 等）。
- **高并发真实延迟测速**：直观展示节点可用性与真实延迟，色标区分优/良/差/超时。
- **卡片快捷一键绑定**：在节点卡片上一键发起端口映射创建，自动预填该节点，简化配置操作。

### 5. 节点漂移安全防护 (Drift Guard)
- 当订阅更新后，若原已绑定的节点被机场服务商重命名或下线，防护引擎会自动将该端口的流量安全降级至 DIRECT 直连并伴随 UI 显式告警，**绝不引发内核配置解析失败或程序崩溃**。

### 6. 内核与客户端全自动运维
- **Mihomo 内核在线升级**：内置内核更新检测器，支持在线检查官方最新发布版本、查看 Release 说明，并支持一键热下载升级。
- **GEO 规则数据库运维**：支持一键检查并在线更新 GeoIP 与 GeoSite 数据库资产。
- **客户端版本更新检测**：内置客户端更新器，支持在线检测 GitHub 最新版本并提供安装包/便携包的一键下载。

### 7. 绿色免安装便携支持 (Portable Mode)
- **免安装、不写注册表**：Windows / Linux 提供开箱即用的 Portable 便携版，程序目录内自带 `.portable` 标记文件，配置与数据随身携带，适合 U 盘随身携带、多实例隔离运行。

---

## 📦 全平台分发包下载指引

| 操作系统 / 架构 | 分发包文件格式 | 产物推荐文件名 | 适用场景与说明 |
| :--- | :--- | :--- | :--- |
| **Windows x64** | `.exe` (NSIS) | `mihomo-multi_1.0.0_x64-setup.exe` | **推荐**。主流 64 位 Windows 用户安装包，带桌面/开始菜单快捷方式与卸载程序 |
| | `.zip` (Portable) | `mihomo-multi_1.0.0_windows-x64-portable.zip` | **推荐**。绿色免安装便携包，解压即用，配置随目录走，与指纹浏览器高度契合 |
| | `.msi` | `mihomo-multi_1.0.0_x64_en-US.msi` | Windows 企业级批量静默部署安装包 |
| **Windows ARM64** | `.exe` (NSIS) | `mihomo-multi_1.0.0_arm64-setup.exe` | 适配 Snapdragon X Elite / Surface Pro 等 Windows on ARM 设备 |
| | `.zip` (Portable) | `mihomo-multi_1.0.0_windows-arm64-portable.zip` | Windows on ARM 绿色免安装便携包 |
| **macOS (Apple Silicon)** | `.dmg` | `mihomo-multi_1.0.0_aarch64.dmg` | 针对 M1/M2/M3/M4 系列 Mac，原生 ARM64 极致性能与低能耗 |
| **macOS (Intel)** | `.dmg` | `mihomo-multi_1.0.0_x64.dmg` | 针对老款采用 Intel 芯片的 Mac 电脑 |
| **macOS (Universal)** | `.dmg` | `mihomo-multi_1.0.0_universal.dmg` | 双架构通用安装包，免去区分架构的识别成本 |
| **Linux x86_64** | `.AppImage` | `mihomo-multi_1.0.0_amd64.AppImage` | **推荐**。全 Linux 发行版通用单文件可执行包，无需安装依赖 |
| | `.deb` | `mihomo-multi_1.0.0_amd64.deb` | Ubuntu / Debian / Deepin 等基于 dpkg 的发行版安装包 |
| | `.tar.gz` (Portable) | `mihomo-multi_1.0.0_linux-x64.tar.gz` | Linux 独立免安装绿色归档包 |

*注：每个分发包的 SHA-256 校验和均记录在同目录的 `SHA256SUMS.txt` 文件中，下载后可核对完整性。*
